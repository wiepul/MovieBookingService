# MovieBookingService
A fun project that lets you book movie tickets.

## Build and Test

1. Create a build directory and run CMake:

```bash
mkdir -p build
cd build
cmake ..
```

2. Build the project:

```bash
cmake --build .
```

3. Run the tests:

```bash
ctest --output-on-failure
```